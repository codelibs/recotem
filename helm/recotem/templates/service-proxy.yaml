apiVersion: v1
kind: Service
metadata:
  name: {{ include "recotem.fullname" . }}-proxy
  labels:
    {{- include "recotem.labels" . | nindent 4 }}
    app.kubernetes.io/component: proxy
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "recotem.proxySelectorLabels" . | nindent 4 }}
