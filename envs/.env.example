# ============================================
# Recotem Environment Variables Reference
# ============================================

# --- PostgreSQL ---
POSTGRES_USER=recotem_user
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=recotem
PGDATA=/var/lib/postgresql/data/pgdata

# --- Django Core ---
# Database connection URL
# Docker: postgresql://recotem_user:password@db:5432/recotem
# Local:  postgresql://recotem_user:password@localhost:5432/recotem
DATABASE_URL=postgresql://recotem_user:change_me_in_production@db:5432/recotem

# Debug mode (set to false in production)
DEBUG=false

# Admin user password (created on first start)
DEFAULT_ADMIN_PASSWORD=change_me_in_production

# Secret key for Django (REQUIRED in production)
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# SECRET_KEY=generate_a_random_secret_key

# Allowed hostnames (comma-separated, REQUIRED in production)
# Server will refuse to start if this contains "*" when DEBUG=false
ALLOWED_HOSTS=localhost,127.0.0.1

# --- Redis ---
# Celery broker (Redis db 0)
# Docker: redis://redis:6379/0
# Local:  redis://localhost:6379/0
CELERY_BROKER_URL=redis://redis:6379/0

# Cache backend (Redis db 2)
CACHE_REDIS_URL=redis://redis:6379/2

# Cache key prefix (useful for multi-tenant or shared Redis)
# CACHE_KEY_PREFIX=recotem

# Optional: Redis password (set on both Redis server and clients)
# REDIS_PASSWORD=your_redis_password

# --- Authentication ---
# JWT access token lifetime in seconds (default: 300 = 5 minutes)
# ACCESS_TOKEN_LIFETIME=300

# --- Rate Limiting ---
# API throttle rates (format: "requests/period", e.g., "100/min", "1000/hour")
# THROTTLE_ANON_RATE=20/min
# THROTTLE_USER_RATE=100/min
# THROTTLE_LOGIN_RATE=5/min

# --- Celery Worker ---
# Task hard time limit in seconds (default: 3600 = 1 hour)
# CELERY_TASK_TIME_LIMIT=3600
# Task soft time limit in seconds (default: 3480 = 58 minutes)
# CELERY_TASK_SOFT_TIME_LIMIT=3480

# --- Model Cache ---
# Number of trained models kept in LRU memory cache (default: 8)
# MODEL_CACHE_SIZE=8

# --- CORS ---
# Comma-separated origins for cross-domain deployments
# CORS_ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com

# --- CSRF ---
# Trusted origins (required if frontend is on a different domain)
# CSRF_TRUSTED_ORIGINS=https://app.example.com

# --- S3 Storage (optional) ---
# Set RECOTEM_STORAGE_TYPE=S3 to use S3-compatible storage
# RECOTEM_STORAGE_TYPE=S3
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_STORAGE_BUCKET_NAME=your_bucket
# AWS_S3_ENDPOINT_URL=https://s3.amazonaws.com
