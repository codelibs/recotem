# When frontend is served by Recotem proxy, defaults are enough.
# Set these only if frontend and backend are deployed separately.

VITE_API_BASE_URL=/api/v1
# Example: wss://api.example.com/ws
# VITE_WS_BASE_URL=
