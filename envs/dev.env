# PostgreSQL
POSTGRES_USER=recotem_user
POSTGRES_PASSWORD=VeryBadPassword
POSTGRES_DB=recotem
PGDATA=/var/lib/postgresql/data/pgdata

# Django
DATABASE_URL=postgresql://recotem_user:VeryBadPassword@localhost:5432/recotem
DEBUG=true
DEFAULT_ADMIN_PASSWORD=very_bad_password

# Celery (Redis as broker)
CELERY_BROKER_URL=redis://localhost:6379/0
CACHE_REDIS_URL=redis://localhost:6379/2

# Redis password (optional)
REDIS_PASSWORD=

# Django secret key (development only — not for production use)
SECRET_KEY=dev-only-insecure-secret-key-do-not-use-in-production-1234567890

# JWT (longer lifetime for development)
ACCESS_TOKEN_LIFETIME=3600
