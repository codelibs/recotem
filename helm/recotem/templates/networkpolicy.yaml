{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "recotem.fullname" . }}-backend
  labels:
    {{- include "recotem.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "recotem.backendSelectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Allow traffic from proxy
    - from:
        - podSelector:
            matchLabels:
              {{- include "recotem.proxySelectorLabels" . | nindent 14 }}
      ports:
        - port: 80
          protocol: TCP
  egress:
    # Allow DNS resolution (kube-system only)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    # Allow connections to PostgreSQL (same namespace pods or external)
    - to:
        - podSelector: {}
      ports:
        - port: {{ .Values.postgresql.port }}
          protocol: TCP
    # Allow connections to Redis (same namespace pods or external)
    - to:
        - podSelector: {}
      ports:
        - port: {{ .Values.redis.port }}
          protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "recotem.fullname" . }}-worker
  labels:
    {{- include "recotem.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "recotem.workerSelectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress: []
  egress:
    # Allow DNS resolution (kube-system only)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    # Allow connections to PostgreSQL (same namespace pods or external)
    - to:
        - podSelector: {}
      ports:
        - port: {{ .Values.postgresql.port }}
          protocol: TCP
    # Allow connections to Redis (same namespace pods or external)
    - to:
        - podSelector: {}
      ports:
        - port: {{ .Values.redis.port }}
          protocol: TCP
{{- end }}
