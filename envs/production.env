# PostgreSQL
POSTGRES_USER=recotem_user
POSTGRES_PASSWORD=CHANGE_ME_to_a_secure_password
POSTGRES_DB=recotem
PGDATA=/var/lib/postgresql/data/pgdata

# Django
DATABASE_URL=postgresql://recotem_user:CHANGE_ME_to_a_secure_password@db:5432/recotem
SECRET_KEY=CHANGE_ME_to_a_random_secret_key
DEBUG=false
# REQUIRED in production: comma-separated list of domain names
ALLOWED_HOSTS=localhost,127.0.0.1
DEFAULT_ADMIN_PASSWORD=CHANGE_ME_to_a_secure_admin_password

# Celery (Redis as broker)
CELERY_BROKER_URL=redis://redis:6379/0

# Cache (Redis db 2)
CACHE_REDIS_URL=redis://redis:6379/2

# CORS (comma-separated origins for cross-domain deployments)
# CORS_ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com

# CSRF trusted origins (required if frontend is on a different domain)
# CSRF_TRUSTED_ORIGINS=https://app.example.com
